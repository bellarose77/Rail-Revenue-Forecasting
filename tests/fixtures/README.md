# tests/fixtures

Placeholder for shared test fixture data (e.g. small frozen synthetic
datasets or expected-output snapshots). Empty until tests are migrated to
use it; created now so the directory exists under version control.
