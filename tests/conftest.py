"""Shared pytest fixtures for the tests/ suite.

Empty for now. Once src/ holds the migrated forecasting package, this file
will provide fixtures such as a cached small synthetic dataset so repeated
tests don't each pay the cost of generate_dataset().
"""
