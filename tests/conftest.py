"""Shared pytest fixtures for the tests/ suite.

Empty for now. The forecasting package now lives in src/forecast_studio.py;
this file will eventually provide fixtures such as a cached small synthetic
dataset so repeated tests don't each pay the cost of generate_dataset().
"""
