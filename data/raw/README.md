# data/raw

Lifecycle stage for data as originally received, before any transformation.

This project has no external raw source — its only dataset is the
`generate_dataset()` synthetic generator (currently in
`modeling/forecast_studio.py`), which writes directly to a reproducible,
already-structured output. This directory is established for lifecycle
completeness and for any future raw input; it is empty for now.
