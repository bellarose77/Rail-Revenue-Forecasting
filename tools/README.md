# tools

Reserved for developer tools and repository maintenance utilities (e.g.
one-off migration or repo-hygiene scripts) that are not part of the
project's own build, test, or data pipeline — those live in `scripts/`
instead.

Empty for now.
